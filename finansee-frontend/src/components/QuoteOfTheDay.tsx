

export default function QuoteOfTheDay() {

    // Check if quote of the day exists in backend server
    // If not, retrieve one from API and save to backend server path
    // Implement in backend to use 'fs'

    return (

        <></>
    )
}